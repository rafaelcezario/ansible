---
- name: Copy files to servers
  hosts: aix

  tasks:
    - name: Copy files
      ansible.builtin.copy:
        #src: /mnt/dnf_aixtoolbox.sh
        src: /mnt/dnf-4.2.17-64_51.src.rpm
        dest: /tmp/
      when: ansible_distribution == "AIX" and ansible_distribution_version == "7.3"

